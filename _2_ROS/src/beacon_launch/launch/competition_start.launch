<launch>

  <!-- Verify that the beacon has the correct IP before starting its rscore -->
  <condition test="$(find beacon_launch)/scripts/verify_beacon_connection.py" />

  <!-- Use basic_start.launch -->
  <include file="$(find beacon_launch)/launch/basic_start.launch"/>
  
</launch>